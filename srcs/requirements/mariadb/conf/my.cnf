[mysqld]

user = mysql
datadir = /var/lib/mysql
port = 3306
socket = /run/mysqld/mysqld.sock

bind-address = 0.0.0.0 # acepta conexiones desde cualquier IP dentro de la red Docker.

max_connections = 100 # máximo de conexiones

character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
